module KeyboardPrototypeNet.Init where
import KeyboardPrototypeNet.Static.Types
import Static.Cmd (Cmd)
import Static.Cmd as Cmd

-- the initial states of each place in this net
initKeyboard :: Keyboard
initKeyboard = Keyboard 


